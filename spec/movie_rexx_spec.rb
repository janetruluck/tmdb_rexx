require 'spec_helper'

describe MovieRexx do
  it 'has a version number' do
    expect(MovieRexx::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
